[package]
name = "shadowai-agent"
version = "0.1.0"
edition = "2024"

[dependencies]
rig = { version = "0.5", features = ["tokio-runtime"] }
tokio = { version = "1", features = ["full"] }
anyhow = "1"
serde_json = "1"
shadowai-tools = { path = "../shadowai-tools" }