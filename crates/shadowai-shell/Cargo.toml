[package]
name = "shadowai-shell"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { workspace = true, features = ["process"] }
thiserror.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = [
  "macros",
  "process",
  "rt-multi-thread",
] }
