# Research Notes Index

Entry point for research documentation in this directory. Each subdirectory contains a focused topic; consult its `index.md` to explore the contents.

## Subdirectories

- [websearch](./websearch/) — Design research for building the `web_search` tool: engine options (Google, Bing, DuckDuckGo, SearXNG), parameter schemas, result normalization, caching & deduplication, error handling & retry strategy, async parallel execution, prompt engineering, testing strategy, and resolved design decisions.